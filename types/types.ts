export interface UserData {
  Notification: string[]; // You can replace 'any' with the actual type if needed
  _id: string;
  Avatar: string;
  closet: any[]; // Replace with actual type if needed
  createdAt: string;
  followers: any[]; // Replace with actual type if needed
  following: any[]; // Replace with actual type if needed
  outfit: any[]; // Replace with actual type if needed
  password: string;
  postCount: number;
  username: string;
}
